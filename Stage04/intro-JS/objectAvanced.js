/*
    AVANÇANDO COM OBJECT
*/

const person = {
    name: "Michael",
    age: 22,
    weight: 68.5,
    isAdmin: true
}

console.log(person); //retorna todas as informações do objeto person

console.log(person.name); //retorna o valor do atributo name do objeto person

console.log(`Essa pessoa se chama ${person.name}`);